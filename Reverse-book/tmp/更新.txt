指针也是一种类型，但比较特殊，声明定义为void*。

第九章 调用规定，Windows系统为什么不用cdecl。
 
第10章 关于引路人

第12章 Dll注入，把工具包当做dll，人就相当于是操作系统。一个房间相当于进程。偏移就相当于是工具包里面的工具放的位置，或者说找到这个工具的步骤。

第14章PDB文件。

导入表检测敏感API

擦除导入表API

内存调试器

远程内存修改合理化------网络通信，文件映射

有一些攻击手法，比如注入/篡改。本质上就是进程之间的通信。
要极速传大数据：选 内存文件映射
要跨机器/跨网段：选 Socket
要父子进程简单传流：选 管道
要传结构化的离散消息：选 消息队列